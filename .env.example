# Environment Configuration
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=production
PORT=3000

# Security (REQUIRED)
# Generate a secure random string for JWT_SECRET (32+ characters)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_minimum_32_characters_please_generate_random_string

# CORS Configuration (REQUIRED for production)
# Comma-separated list of allowed origins (your frontend domains)
# Example: https://myapp.com,https://www.myapp.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Printful API (Required for automated product listing)
# Get from: https://www.printful.com/dashboard/store → Settings → API
PRINTFUL_API_KEY=your_printful_api_key_here

# Canva API (Optional - for automated design generation)
# Get from: https://www.canva.com/developers
CANVA_API_KEY=your_canva_api_key_here
CANVA_BRAND_TEMPLATE_ID=your_brand_template_id_here

# Optional Integrations
STRIPE_API_KEY=your_stripe_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Error Monitoring (Recommended for production)
# Get from: https://sentry.io (free tier available)
SENTRY_DSN=your_sentry_dsn_here

# ============================================
# MASTER AUTOMATION CONFIGURATION
# ============================================

# Server URL (for automation scripts)
# Local: http://localhost:3000
# Vercel: https://your-app.vercel.app
# Render: https://your-app.onrender.com
SERVER_URL=http://localhost:3000

# Cron Job Security (REQUIRED for Vercel cron jobs)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your_cron_secret_here_generate_random_string

# Automation Settings
MAX_PRODUCTS=10                  # Max products to process per automation run
GENERATE_DESIGNS=true            # Enable AI design generation
CREATE_LISTINGS=true             # Enable store listing creation
GENERATE_MARKETING=true          # Enable marketing content generation
GLOBAL_TRENDING=true             # Enable global trending discovery

# Global Trending Regions (comma-separated country codes)
# Available: US,GB,CA,AU,DE,FR,JP,BR,IN,MX
TRENDING_REGIONS=US,GB,CA,AU,DE,FR,JP,BR,IN,MX

# Automation Schedule (cron format: minute hour day month weekday)
# Recommended: 0 3 * * * (Every day at 3:00 AM - optimal for overnight profit generation)
# Examples:
#   0 3 * * *     = Every day at 3:00 AM (RECOMMENDED)
#   0 9 * * *     = Every day at 9:00 AM
#   0 */6 * * *   = Every 6 hours
#   0 9 * * 1,3,5 = Monday, Wednesday, Friday at 9:00 AM
AUTOMATION_SCHEDULE=0 3 * * *
